{
  "registry": "soma-lang.dev/repo",
  "description": "The Soma package registry — a sparse HTTP index. soma add <name> resolves GET /<name>.json.",
  "packages": [
    { "name": "matrix", "latest": "0.2.0", "description": "numpy-style linear algebra: inverse, solve, lstsq, arange, stacking, statistics, elementwise lambdas." }
  ]
}

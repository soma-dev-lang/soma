[package]
name = "soma_native"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
rug = { version = "1", default-features = false, features = ["integer"] }

[profile.release]
opt-level = 3
overflow-checks = true
panic = "unwind"

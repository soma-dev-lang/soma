// Connection to Exchange via TCP signal bus

cell Connection {

    on connect_exchange() {
        // Connect to exchange's signal bus (port 8082 = HTTP 8080 + 2)
        return link("localhost:8082")
    }
}

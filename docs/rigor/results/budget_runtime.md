# Budget runtime validation results

Generated by `docs/rigor/bin/bench_budget_runtime.sh` on 2026-04-10.

Each row is one cell that passes the V1.4 budget proof. The
harness extracts the proven peak from `soma check`, runs the
cell under a real OS memory cap set to `proven_peak + 32 MiB`,
and asserts that the run completes without OOM or crash.

| cell | proven peak | runtime cap | result |
|------|------------:|------------:|:------:|
| trivial | 24.08 MiB | 57 MiB | ✅ |
| many_handlers | 24.00 MiB | 56 MiB | ✅ |
| state_machine | 24.08 MiB | 57 MiB | ✅ |

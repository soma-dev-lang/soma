// T33: JIT - Lambda: filter(x => x > 3)
cell T33 {
    on run() {
        let nums = list(1, 2, 3, 4, 5, 6)
        let big = nums |> filter(x => x > 3)
        print("filtered: {big}")
        return "done"
    }
}

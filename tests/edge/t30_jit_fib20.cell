// T30: JIT - fib(20) recursive
cell T30 {
    on fib(n: Int) {
        if n <= 0 { return 0 }
        if n == 1 { return 1 }
        return fib(n - 1) + fib(n - 2)
    }
    on run() {
        let result = fib(20)
        print("fib(20): {result}")
        return "done"
    }
}

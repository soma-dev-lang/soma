// Test: soma verify reachability - all states reachable
cell Reachable {
    memory {
        items: Map<String, String> [persistent, consistent]
    }
    state workflow {
        initial: draft
        draft -> review
        review -> published
        * -> cancelled
    }
    on test() { return "ok" }
}

// Test: soma.toml [verify] properties
cell TomlVerify {
    memory {
        items: Map<String, String> [persistent, consistent]
    }
    state workflow {
        initial: draft
        draft -> submitted
        submitted -> approved
        approved -> shipped
        shipped -> delivered
        * -> cancelled
    }
    on test() { return "ok" }
}

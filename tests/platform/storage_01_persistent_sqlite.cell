// Test: persistent storage creates SQLite file
cell PersistStore {
    memory {
        data: Map<String, String> [persistent, consistent]
    }
    on put(key: String, value: String) {
        data.set(key, value)
        return value
    }
    on get(key: String) {
        return data.get(key)
    }
}

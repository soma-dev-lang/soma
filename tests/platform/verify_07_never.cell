// Test: never temporal property - unreachable state
cell NeverCheck {
    memory {
        items: Map<String, String> [persistent, consistent]
    }
    state workflow {
        initial: safe
        safe -> running
        running -> done
    }
    on test() { return "ok" }
}

cell Test {
    on run() {
        let nums = list(1, 2, 3, 4, 5)
        let total = 0
        for n in nums {
            total = total + n
        }
        print(total)
        return "done"
    }
}
